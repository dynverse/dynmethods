name: ouijaflow
parameters:
  iter:
    type: integer
    lower: 2
    upper: 50000
    default: 1000
    distribution: exponential
    rate: 0.0001
input:
  format: text
  required:
    - expression
    - features_id
output:
  format: text
  outputs:
    - linear_trajectory
    - timings
