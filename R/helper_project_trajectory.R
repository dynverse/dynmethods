project_cells_to_segments <- function(
  cluster_network,
  cluster_space,
  sample_space,
  sample_cluster = NULL,
  num_segments_per_edge = 100,
  milestone_rename_fun = NULL
) {
  .Deprecated("add_cluster_projection_to_wrapper", "dynutils")
}
